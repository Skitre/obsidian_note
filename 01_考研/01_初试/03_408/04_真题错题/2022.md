5、哈夫曼编码和定长编码树形
8、B 树删除
10、二路归并的本质

14、IEEE 754 算错
16、cache 组相连，先根据组号找到组，然后使用比较器找组内的具体行，所以需要路数位的比较器
22、多处理器

23、多道程序辨析
24、OS 初始化时必须创建中断向量表，与文件相关的在文件系统初始化时创建
25、t=0 时刻调度（之前做了一个 t=0 时刻有调度延迟）
28、读文件，文件在 IO 所以会变为阻塞态
30、页缓冲队列：淘汰的页会缓存下来，先不回磁盘，但是也是缺页，不影响缺页率
31、系统调用过程中硬件保存断点和程序状态字，并且完成用户到内核的转化


45、
![[Pasted image 20251124152328.png]]