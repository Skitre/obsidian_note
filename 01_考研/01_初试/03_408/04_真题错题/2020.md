1、下标从 1 开始
2、至少需要多少个存储单元-》考虑最多需要存放多少个
10、B 树节点范围
11、插入排序和简单选择排序

16、指令中除了操作码和地址码，还有寻址方式位
22、DMA 周期挪用，CPU 可以访问主存

24、文件磁盘空间管理索引分配支持长度可变和随机访问

36、停等协议，接收到完整数据帧后再发送确认帧
37、CSMA/CA 的几个 ifs
38、TCP 的 RTT（注意要求时间最长，所以直接拥塞避免）
40、DNS 过程


#### 46
![[Pasted image 20251122185116.png]]