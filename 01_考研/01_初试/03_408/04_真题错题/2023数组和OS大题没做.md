3、三元组中除了三元组还需要存储 M 的行数和 M 的列数，不需要储存非零元素的行数和列数
7、B 树插入操作可能增加树的高度、删除操作一定会导致叶结点的变化
9、散列表，删除后并非真的删除，而是打上删除 tag（如果真的删除需要去进行移位操作）
计组

16、OF CF，OF 是有符号位的溢出标志，可以直接根据计算结果真值范围来判断，CF 是无符号数操作，在有符号数运算时需要将其看成无符号数去判断（不能直接根据带符号数真值）
21、外部设备通过中断控制器向 CPU 发送中断请求信号而不是中断结束信号，中断结束通常由指令完成

26、执行系统调用完成后会返回用户态，
31、磁盘的索引代表着磁盘有多少个磁盘块，也就是磁盘的大小，和文件打开表里的链接数分清楚

33、少算了一个分组的发送（图画错了）
38、路由器转发的分组的 IP 地址是路由器发送端口的 IP（有时图中不给，需要自己通过子网划分自己算，阴险）



47、FTP 传送数据过程