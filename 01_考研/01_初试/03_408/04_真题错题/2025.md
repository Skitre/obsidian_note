4、链式树的根中保存的是最后计算的运算符（图表示二叉树运算）
9、只要散列表不满，线性探查再散列一定能找到一个空闲位置
11、根据序列判断排序算法


12、short 转 unsign int
15、小端地址
21、适合 DMA 输入输出的设备-进行大量数据传输的设备-打印机数据量很小
22、DMA 传送结束后会发送中断请求


25、用链表实现的优先级队列，插入是 o（N）因为要按值找到插入的位置，选出进程只需要选队头的元素即可，所以是 O（1）
27、确定进程运行所需的最少页框数时，需要考虑指令系统支持的寻址方式，因为提到了最少。代码段长只是能确定需要的总的框数
30、内存映射文件
31、文件系统用于外存空间使用情况-空闲情况-FAT
32、文件系统能为温彻斯特硬盘和固态硬盘提供的功能是确定盘块大小


33、具体计算一个链路中电路交换，报文交换，分组交换传输的速率
38、TCP 拥塞控制
