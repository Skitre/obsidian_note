8、根据树型判断是否为折半查找树，主要是看取中值的时候规则一不一致
11、更换数据结构排序算法时间复杂度会降低的是（需要进行随机访问）

13、数据未对齐的时候要多一次访存
20、PCI-Express×16 采用串行传输方式、总线之间须通过桥接器相连
21、IO 端口是 CPU 和设备之间的交界面，所以数据传送经常发生在 CPU 和寄存器之间

25、空闲分区表
31、不同进程可以对同一文件进行硬链接，但是因为不同进程对文件的读写需要不一致所以指针位置也不同、
32、DMA 从磁盘读取数据到内存的流程

34、奈式准则和香农定理
35、802.11 的三个地址



