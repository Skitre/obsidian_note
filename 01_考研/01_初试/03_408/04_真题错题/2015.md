7、根据树形判断是否为折半查找关键字比较序列
9、基数排序**元素移动次数**与初始次序**无关**
补充：
**比较次数**与序列初态**无关**的算法是：简单选择排序、基数排序、折半插入排序、堆排序
**比较次数** 与序列初态 **有关** 的算法是：归并、快速排序、直接插入排序、冒泡排序、希尔排序
**排序趟数** 与序列初态 **无关** 的算法是：插入排序、简单选择排序、归并排序、基数排序
**排序趟数** 与**序列初态** 有关的算法是：冒泡排序、快速排序
（此处的冒泡排序为优化版）
![[Pasted image 20251125142627.png]]

12、计算机硬件能直接执行的语言（硬件描述语言程序**不是**）
21、程序中断方式中，数据是 CPU 先从主存中读出，再传通过 IO 指令传送给 IO 端口，IO 端口不会获得内存地址

26、死锁避免（银行家算法）只检查是否处于安全状态，若不处于则**放弃试探分配**，并不涉及限制顺序
30、固定分配、可变分配、全局置换、局部置换
31、注意位示图每一位是一个 bit，如果问到在哪个字节要除 8
