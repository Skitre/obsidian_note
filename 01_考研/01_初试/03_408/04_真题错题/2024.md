11、败者树冠军结点保存的内容是最小关键字所在的**归并段段号**

14、整数运算比浮点数运算快
16、主存-外村采用的是页面映射（不是直接映射，因为有置换算法）
17、采用页式虚拟存储管理方式，在 TLB （用于虚拟地址向物理地址转换）设置 tag 位的时候，用虚拟地址而不是物理地址，而 cache 用的是物理地址
20、总线带宽，既要考虑正常传送也要考虑突发传送，突发传送带宽不一定大
21、终端屏蔽字用于开关中断，只可确定正在进行的中断比请求的终端优先级高/低，不能确认不同终端具体优先级关系


25、切换进程时要 1、更新 PC 2、更新栈基址寄存器 3、更新页表基址寄存器
31、键盘中断服务例程执行结束时，所输入的数据存放位置是内核缓冲区
32、CSCAN 不掉头


36、设置的 NAV 值是 SIFS + 数据发送时间 + SIFS + ACK 传输时延
