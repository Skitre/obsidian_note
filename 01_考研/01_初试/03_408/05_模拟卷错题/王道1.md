4、节点数与多少个二叉树：卡特兰数
5、哈夫曼树给定层数，最多可以编码多少个数：最高层尽量全是叶子
6、强连通图最少有 n 条边（圈）
8、二叉排序树中序遍历就是结点按序排列
11、快排最好的情况：每次将区间分为大小相近的两个区间

14、IEE 754 尾数只有 23 位，对阶会丢失精度
17、五段式流水线 lw 和 sw 只在 IF 和 MEM 阶段访存
20、数据冒险加 nop（ID 和 WB 进行寄存器操作，IF，MEM 阶段对贮存操作，EX 阶段对 ALU 操作）
22、优先级高的中断再进行时，不会检测到低优先级的中断，因为被屏蔽了

27、wait signal 可以解决所有互斥问题   
	程序的顺序执行在独立运行时可以再现，并发时不可再现
31、
![[Pasted image 20251126161408.png]]

35、SR 协议，发送方能发送的数据帧数量为**超时需要重传的+发送窗口空闲的**
38、分组的排序和重组，IP 进行重组（有相同的源 IP 目的 IP），传输层负责排序（例如 TCP 中都有序号）
39、TCP 的四个计时器，超时重传，持续计时器，保活计时器，时间等到计时器，除了保活计时器都是客户的
