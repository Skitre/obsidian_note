4、矩阵元素的下标从 1 开始还是从 0 开始
5、通过边和结点的关系判断森林中树的个数
8、Djkstra 少看到一条边

14、大端小端
15、通过程序计算缺失率，需要看清程序中访问了几次该地址元素，图中就是两次
![[Pasted image 20251125145537.png]]
**16、**
20、单周期处理器不能采用单总线，不同设备比如控制命令和数据传送回抢占总线，而因为一条指令只有一个时钟周期，始终不发生变化，控制信号也不变

26、改进型 CLOCK 置换算法，先换出未被访问被修改的，再被换出被访问未被修改的即先 01 在 10
27、TSL
29、工作集和驻集
32、管程


TCP 编号面向字节