3、数组计算
4、不同平衡树的数量
9、拉链法装填因子，可能大于 1
11、堆排序，注意是逐个插入开始还是已经有一个序列

13、float 格式转成 longlong 是按值转
14、正数是否溢出

24、中断处理程序执行期间进程调度器可能将 CPU 分配给其他进程
30、LRU 通过维护一个双向链表和哈希表实现，哈希表是为了加快访问
31、远程挂载系统会覆盖该文件夹内已有文件的访问

36、CTS 和 RTS 不能完全解决隐蔽战问题