14、IEEE 最小的规格化正数，阶码是 1 而不是 0
15、为什么只有操作数部分是小端 ？
18、地址是 16 进制.。
21、地址线复用不能提高数据传输率

24、在算周转时间时要算上周转时间，尤其是 t=0 时刻的周转时间
28、管程的条件变量 x.wait 执行阻塞进程，x.signal 唤醒阻塞队列的进程
32、实现同步互斥的方法（硬件无法实现让权等待，信号量机制引入阻塞队列能够实现）

40、无需转换可直接由 SMTP 传送的是 ASCII 文本文件


47、计算子网还有多少设备可以分配，不要忘了算上路由器
